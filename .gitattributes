# Large ML model — use Git LFS (required for GitHub; file is ~90 MB)
public/models/breed_classifier/breed_classifier.onnx filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text
